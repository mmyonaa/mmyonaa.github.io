node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
logs
*.log
npm-debug.log*

# Claude Code / MCP (개인 개발 환경 설정)
.mcp.json
.claude/
.serena/
.playwright-mcp/

# 실제 고객사명 포함 — 공개 배포/저장소에서 제외 (로컬 보관)
public/portfolio/resume.html

# 개발 노트·내부 작업 정리(개인 참고용) — 저장소/배포 제외
# 이력서·자소서 HTML 원본은 claude-shared/career/documents/ 로 이관했다(2026-09-24).
# 이 레포는 공개(mmyonaa.github.io)라 문안 원본과 그 수정 이력을 두지 않는다 — 사이트는 PDF 만 링크한다.
docs/*

# nie 다이어그램: draw.io 원본·중간 파일은 비배포 (렌더된 PNG만 포함)
public/projects/nie/**
!public/projects/nie/flow.png
!public/projects/nie/flow-light.png
!public/projects/nie/flow-en.png
!public/projects/nie/flow-en-light.png
!public/projects/nie/kakao-flow.png
!public/projects/nie/kakao-flow-light.png
!public/projects/nie/kakao-flow-en.png
!public/projects/nie/kakao-flow-en-light.png
# 앱 스크린샷 (스마트폰 목업 슬라이더용)
!public/projects/nie/nie-stamp-board.png
!public/projects/nie/nie-content-detail.png
!public/projects/nie/nie-code-entry.png

# 포트폴리오 PDF 는 로컬 생성물(`pnpm portfolio:pdf`) — 사이트에서 링크하지 않으므로 제외.
# (이력서 PDF 는 헤더 버튼이 링크하므로 public/resume-*.pdf 는 계속 추적합니다.)
public/portfolio-*.pdf

# 출시 전 제품 발표 자료는 apply/ 내부에만 보관합니다.

# 지원서 제출용 묶음(이력서·포트폴리오 PDF) — 개인 보관, 저장소/배포 제외
apply/

# 세션 인계 노트(작업 로그) — 개인 보관, 저장소/배포 제외
handoff/
